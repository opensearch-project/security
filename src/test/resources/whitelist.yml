---
_meta:
  type: "whitelist"
  config_version: 2

#this name and type name must be whitelisting_settings
whitelisting_settings:
  whitelisting_enabled: false
  whitelisted_APIs:
    /_cat/nodes:
      - GET
    /_cat/plugins:
      - GET
    /_cluster/health:
      - GET
    /_cluster/settings:
      - GET
