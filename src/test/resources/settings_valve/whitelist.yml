---
_meta:
  type: "whitelist"
  config_version: 2

#this name must be config
config:
  enabled: false
  requests:
    /_cat/nodes:
      - GET
    /_cat/plugins:
      - GET
    /_cluster/health:
      - GET
    /_cluster/settings:
      - GET
