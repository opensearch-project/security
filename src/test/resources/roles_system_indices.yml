---
_meta:
  type: "roles"
  config_version: 2

opendistro_security_all_access:
  description: "All access role"
  cluster_permissions:
    - '*'
  index_permissions:
    - index_patterns:
        - '*'
      allowed_actions:
        - '*'

extensions_role:
  description: "ExtensionsRole"
  cluster_permissions:
    - '*'
  index_permissions:
    - index_patterns:
        - '*'
        - 'config1'
      allowed_actions:
        - '*'
        - 'system:admin/system_index'